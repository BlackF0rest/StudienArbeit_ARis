# Onboard UI launch route manifest

Launch-approved routes:

- `/`
- `/Navigation`
- `/Teleprompter`
- `/Messages`

Excluded from launch scope:

- `/Debug` (operator/debug only)
- `/dev/*` (development namespace)
